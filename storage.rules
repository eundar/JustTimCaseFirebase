rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Shared firm-wide documents: any signed-in attorney can read/write any file.
    match /documents/{ownerId}/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
